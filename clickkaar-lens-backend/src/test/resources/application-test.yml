spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/clickkaar_lens_test?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret-test-secret
    expiration: 900000
    refresh-expiration: 604800000
